# AltTester (optional)

Use only if the project already has an AltTester-instrumented Unity build and driver setup, or if the user explicitly chooses to add it.

Good for object-level Unity UI/runtime control and component state access. It can strengthen black-box/runtime execution but is not required for source-based native testing.

If unavailable, continue in `NATIVE` mode.
