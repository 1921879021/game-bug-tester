# Optional adapters

These are enhancements, **not dependencies**.

Only use one when it is already installed/configured or the user explicitly asks to add it.

- `alttester.md` — Unity object-level automation.
- `airtest-poco.md` — black-box/image + supported UI hierarchy automation.

The core skill must remain useful when this directory is ignored completely.
