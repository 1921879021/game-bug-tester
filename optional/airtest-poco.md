# Airtest / Poco (optional)

Use only if already available or explicitly requested.

Airtest can help control image/coordinate-driven black-box builds. Poco can provide hierarchy-based UI control in supported integrations.

If neither is installed and only a compiled build exists, switch to `BLACKBOX_ASSISTED`; do not auto-install them.
